// Instalacao Node no Windows //

//--> O download do Node.js eh feito no site oficial: nodejs.org;
//--> Vamos baixar um arquivo .msi, que eh o instalador;
//--> Eh interessante saber que o npm vem junto do Node;
//--> Apos a instalacao podemos testar o node e o npm em um terminal, para validar a instalacao;
//--> Note que os binarios precisam estar no PATH, este passo esta na instalacao do Node;
